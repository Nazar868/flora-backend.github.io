# Server
PORT=3000

# PostgreSQL connection (Render / any cloud provider)
# Full connection string, e.g.:
# postgresql://USER:PASSWORD@HOST:5432/DBNAME
DB_URL=postgresql://user:password@host:5432/flora

# Set to "true" only if your provider requires SSL (Render does)
DB_SSL=true
